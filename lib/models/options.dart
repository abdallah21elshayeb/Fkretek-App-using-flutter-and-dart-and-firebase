import 'package:flutter/material.dart';

class Options {
  final String id;
  final String title;
  final Color color;

  const Options({
    required this.id,
    required this.title,
    required this.color,
  });
}
